// Single source of truth for the app version. Shown on the login/inicio screen and
// stamped into every record's version_app — parity with the Power Apps lbl_versionApp_SL
// label that fed the AppVersion global. Bump this on each production release.
// Format mirrors the original: v<YYYYMMDD>_<semver>. Previous (Power Apps): v20260622_1.3.2.
export const APP_VERSION = 'v20260827_1.1.16';
